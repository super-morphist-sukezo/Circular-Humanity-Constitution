[project]
name = "circular-humanity-const"
version = "1.0.0"
description = "AI constitution toolkit for Earth-system circularity and human passion"
license = {text = "CC BY-SA 4.0"}
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
langchain = ["langchain-core"]
llamaindex = ["llama-index-core"]

[tool.setuptools]
packages = ["circular_humanity_const"]
